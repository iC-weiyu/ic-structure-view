---
name: ic-structure-view
description: Use when iC asks to plan, review, or refine how a molecular structure should be visually presented, including proteins, complexes, ligands, binding sites, MD frames, and structural comparisons. Covers visual hierarchy, representation, color, focus, comparison logic, and evidence boundaries. Do not use for general scientific charts; when PyMOL or PML implementation is explicitly requested, also use icpml-skill.
---

# iC 分子结构视图规划

## 目标与边界

为分子结构视图建立科学含义明确、重点清楚、信息密度克制的整体方案。输出可以是视图设计、对象角色、表示方式、配色和镜头建议，但不生成 PML、不规定 PyMOL 命令，也不负责文件打包或渲染验收。

本技能适用于蛋白、复合物、短肽、小分子、辅因子、底物、结合位点、MD 代表帧和结构比较。它不适用于 RMSD 曲线、热图、统计图、示意流程图或一般论文 Figure；这些任务应使用相应的科学作图或图形设计技能。

主要服务蛋白体系。遇到核酸、膜、材料或其他非典型体系时，如果表示方式或关注区域不能从请求中可靠判断，先询问一个会改变视图结论的关键问题，不擅自套用蛋白规则。

## 规划流程

1. 确认用户想比较或解释什么，以及视图是探索、教学、汇报还是论文用途。
2. 从实际体系和问题中分配背景、关注对象、相互作用区域与比较状态，不假定固定分子组合、链名或残基编号。
3. 为各角色选择表示、颜色、透明度和信息密度，确保重点不会被重复背景或无关细节遮挡。
4. 确定比较是否需要共同参照、统一镜头、分面、叠合或多个状态，并写清这些选择能支持什么、不能支持什么。
5. 若用户明确要求 PyMOL/PML 交付，将方案交给 `icpml-skill` 落实和验收。

## 视觉角色

- **背景/支架**：提供空间参照的主体，通常采用低饱和、低竞争度的表示；只在有助于解释时增加局部 surface 或其他层次。
- **关注对象**：当前问题真正要观察的分子、区域或构象。短 peptide、小分子、辅因子和底物可以完整显示非氢原子；大分子只突出相关局部。
- **反应/相互作用区域**：只保留能回答当前问题的关键侧链、原子、接触或测量，不机械展开“附近所有残基”。
- **比较状态**：使用稳定、可区分且不暗示未证实结论的配色。已有验收配色或整套图稿规范时优先沿用。
- **独立区域**：当 groove、domain 或 site 需要单列时，先明确它与背景是否互斥，避免同一原子在同层对象中无意义重复。

## 视觉语言

- 默认建立一个低竞争度背景和清晰的关注层，不让多个重复的完整结构遮挡核心区域。
- 水、离子、氢原子和无关配体默认不抢占画面；与当前科学问题直接相关者应保留。
- 标签、距离和虚线默认克制，只在它们能帮助回答问题时使用。
- 镜头围绕可见背景与全部关注对象，不让整盒、隐藏内容或无关远端区域决定构图。
- 小型比较优先使用简单、可解释的层级；状态很多时才引入分组、分面或更复杂的组织方式。
- 可读性优先于展示所有信息；同一张视图无法同时承担的任务应拆成互补视图。

## 比较与证据边界

- 结构叠合前要明确共同参照和拟合选择；不同拟合基准可能改变局部位移的视觉印象。
- 颜色、距离、接触和单帧构象可以用于描述差异或提出候选解释，但不能单独证明自由能、动力学、收敛、状态归属、过渡路径或因果机制。
- MD 代表帧必须说明选择依据；medoid、端点、聚类中心和人为挑选帧不能互相替代。
- 视觉密度、透明度和镜头都是展示决策，不应被表述为新的定量证据。

## 输出格式

根据请求给出最小充分的视图方案，通常包括：

- 视图目的与比较对象；
- 背景、关注对象、相互作用区域和比较状态；
- 各角色的表示、配色和可见性；
- 对齐、镜头、标签或测量建议；
- 该视图能够支持的结论及证据限制。

仅做规划时不创建脚本、PML、图片或目录。明确要求实施 PyMOL/PML 时，再叠加 `icpml-skill`。
